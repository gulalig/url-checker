services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: url-checker-backend
    env_file:
      - ./backend/.env
    ports:
      - "3000:3000"
    restart: unless-stopped